<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".NetSpeedApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NetSpeedIndicator"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.NetSpeedIndicator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.NetSpeedForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse|dataSync">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Real-time Network Speed Indicator and Traffic Monitor" />
        </service>

        <!-- Floating Speed Bubble Overlay Service -->
        <service
            android:name=".service.FloatingBubbleService"
            android:exported="false" />

        <!-- Android Quick Settings Dropdown Tile -->
        <service
            android:name=".service.SpeedTileService"
            android:exported="true"
            android:icon="@drawable/ic_speed_indicator"
            android:label="@string/tile_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Home Screen App Widget.
             exported="false": the custom WIDGET_REFRESH / WIDGET_TOGGLE_SERVICE
             actions are only ever sent by this app's own PendingIntents (set via
             setOnClickPendingIntent). exported="true" allowed ANY third-party
             app to broadcast those actions and spam widget refreshes / toggle
             the monitor service. APPWIDGET_UPDATE from the system launcher is
             still delivered: the system is exempt from exported gating. -->
        <receiver
            android:name=".widget.NetSpeedWidgetProvider"
            android:exported="false"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.onlasdan.netnet.action.WIDGET_REFRESH" />
                <action android:name="com.onlasdan.netnet.action.WIDGET_TOGGLE_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/net_speed_widget_info" />
        </receiver>

        <!--
            AlarmManager receiver — replaces the two WorkManager Workers
            (NetSpeedKeepAliveWorker + DailyUsageSummaryWorker). Handles
            the periodic keep-alive alarm and the daily summary alarm.
        -->
        <receiver
            android:name=".work.NetSpeedAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.onlasdan.netnet.action.WATCHDOG_ALARM" />
                <action android:name="com.onlasdan.netnet.action.DAILY_SUMMARY_ALARM" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
